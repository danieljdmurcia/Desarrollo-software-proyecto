name: CI - Pruebas Automatizadas

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Instalar dependencias
        run: |
          pip install fastapi uvicorn jinja2 python-multipart sqlalchemy pytest httpx
      - name: Correr pruebas
        run: |
          pytest tests/ -v

      - name: Instalar dependencias
        run: |
          pip install fastapi uvicorn jinja2 python-multipart sqlalchemy pytest httpx

      - name: Correr pruebas
        run: |
          pytest tests/ -v
